# Environment variables for Plastic Surgeon Assistant PWA
# Copy to .env and fill in values. Restart dev server after changes.

# OpenAI API key for AI-assisted CME generation and study recommendations
VITE_OPENAI_API_KEY=sk-your-key-here

# Backend API base URL (if/when backend is integrated)
VITE_API_BASE_URL=https://api.example.com

# VAPID public key for Web Push (if using push notifications via service worker)
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key
